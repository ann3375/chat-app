export enum InputType {
  text = 'text',
  email = 'email',
  password = 'password',
  file = 'file',
  textarea = 'textarea',
  select = 'select',
}

export enum InputId {
  username = 'username',
  password = 'password',
  file = 'file',
  messageText = 'messageText',
  captcha = 'captcha',
}

export enum InputSize {
  large = 'large',
  medium = 'medium',
}
