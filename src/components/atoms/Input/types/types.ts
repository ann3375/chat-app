export enum InputType {
  text = 'text',
  email = 'email',
  password = 'password',
  file = 'file',
  textarea = 'textarea',
}

export enum InputId {
  username = 'username',
  password = 'password',
  file = 'file',
  messageText = 'messageText',
}
