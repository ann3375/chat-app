export enum SCREENS {
  SCREEN_HOME = '/',
  SCREEN_LOGIN = '/login',
  SCREEN_CURRENT_DIALOG = '/dialogs/:id',
  SCREEN_DIALOGS = '/dialogs',
}

export enum Pages {
  login = 'login',
  chat = 'chat',
}
